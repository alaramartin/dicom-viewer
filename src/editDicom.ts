import * as vscode from 'vscode';

// todo: add function that edits and replaces the dicom file
export function saveDicomEdit(tag:string, newValue:string, filepath:string) {

}

// todo: add function that removes a tag and replaces the dicom file
export function saveTagRemoval(tag:string, filepath:string) {

}